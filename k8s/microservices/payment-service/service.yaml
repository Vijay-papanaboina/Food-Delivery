apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: food-delivery
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: payment-service
