apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: food-delivery
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      imagePullSecrets:
        - name: ghcr-login-secret
      containers:
        - name: notification-service
          image: ghcr.io/vijay-papanaboina/food-delivery-notification-service:latest
          # imagePullPolicy: Never
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            - name: SERVICE_NAME
              value: "notification-service"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: NODE_ENV
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: KAFKA_BROKERS
            - name: KAFKA_CLIENT_ID
              value: "notification-service"
            - name: KAFKA_GROUP_ID
              value: "notification-service-group"
            - name: KAFKAJS_NO_PARTITIONER_WARNING
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: KAFKAJS_NO_PARTITIONER_WARNING
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
            - name: REFRESH_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REFRESH_TOKEN_SECRET
            - name: FRONTEND_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: FRONTEND_URL
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /api/notification-service/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/notification-service/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
